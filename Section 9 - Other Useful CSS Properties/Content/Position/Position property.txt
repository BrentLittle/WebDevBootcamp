Position
    Sets how an element is positioned in a document
    The top right bottom and left properties are set

Static
    DEFAULT
    position : static;

Relative
    allows us to offset the element relative to itself
    position:relative;
    top:100px;

Absolute
    Element is removed from the normal document flow so no space is left for the element in the page layout
    position:absolute;
